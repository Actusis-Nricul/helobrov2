local Players = game:GetService("Players")
local UIS = game:GetService("UserInputService")

local LocalPlayer = Players.LocalPlayer

local gui = Instance.new("ScreenGui")
gui.Name = "PotassiumExecutorIsGOAT"
gui.ResetOnSpawn = false
gui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0,500,0,350)
frame.Position = UDim2.new(0.5,-150,0.5,-90)
frame.BackgroundColor3 = Color3.fromRGB(50,65,95)
frame.Parent = gui

local topBar = Instance.new("Frame")
topBar.Size = UDim2.new(1,0,0,30)
topBar.BackgroundColor3 = Color3.fromRGB(95,50,99)
topBar.Parent = frame

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1,-30,1,0)
title.BackgroundTransparency = 1
title.Text = "Piggy Auto Farm As Piggy (2k+ per Hour)"
title.TextColor3 = Color3.new(1,1,1)
title.Parent = topBar

local close = Instance.new("TextButton")
close.Size = UDim2.new(0,30,1,0)
close.Position = UDim2.new(1,-30,0,0)
close.Text = "X"
close.Parent = topBar

local usernameBox = Instance.new("TextBox")
usernameBox.Size = UDim2.new(1,-20,0,35)
usernameBox.Position = UDim2.new(0,10,0,60)
usernameBox.PlaceholderText = "Enter Username"
usernameBox.Text = ""
usernameBox.Parent = frame

local tpButton = Instance.new("TextButton")
tpButton.Size = UDim2.new(1,-20,0,35)
tpButton.Position = UDim2.new(0,10,0,120)
tpButton.Text = "Teleport To Player"
tpButton.Parent = frame

local autoTpButton = Instance.new("TextButton")
autoTpButton.Size = UDim2.new(1,-20,0,35)
autoTpButton.Position = UDim2.new(0,10,0,180)
autoTpButton.Text = "Auto Teleport: OFF"
autoTpButton.Parent = frame

local note = Instance.new("TextLabel")
note.Size = UDim2.new(1,-20,0,80)
note.Position = UDim2.new(0,10,0,240)
note.BackgroundTransparency = 1
note.TextColor3 = Color3.fromRGB(253,0,0)
note.TextScaled = true
note.Font = Enum.Font.SourceSansBold
note.Text = "WARNING: Choose Player and then pick any map, go in private server, pick hard mode and big traps, then put in your alt username without errors, and then press auto teleport, it will constantly kill your alt every couple seconds and leave it on over night and you will be stacked (make sure alts dont leave, use my other script to automaticlly start the game if it ends after the 10 minute mark, my script can be found on the PiggyHub script on versions V4 and above, You Must Have 3 Players in game, so round doesnt end when you get a kill."
note.Parent = frame

close.MouseButton1Click:Connect(function()
	gui:Destroy()
end)

local dragging = false
local dragInput
local dragStart
local startPos

topBar.InputBegan:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseButton1 then
		dragging = true
		dragStart = input.Position
		startPos = frame.Position

		input.Changed:Connect(function()
			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

topBar.InputChanged:Connect(function(input)
	if input.UserInputType == Enum.UserInputType.MouseMovement then
		dragInput = input
	end
end)

UIS.InputChanged:Connect(function(input)
	if input == dragInput and dragging then
		local delta = input.Position - dragStart

		frame.Position = UDim2.new(
			startPos.X.Scale,
			startPos.X.Offset + delta.X,
			startPos.Y.Scale,
			startPos.Y.Offset + delta.Y
		)
	end
end)

local function teleportToPlayer()
	local username = usernameBox.Text

	if username == "" then
		return
	end

	local target = Players:FindFirstChild(username)

	if not target then
		return
	end

	local myChar = LocalPlayer.Character
	local targetChar = target.Character

	if not myChar or not targetChar then
		return
	end

	local myRoot = myChar:FindFirstChild("HumanoidRootPart")
	local targetRoot = targetChar:FindFirstChild("HumanoidRootPart")

	if myRoot and targetRoot then
		myRoot.CFrame = targetRoot.CFrame
		end
	end

tpButton.MouseButton1Click:Connect(teleportToPlayer)

local autoTp = false

autoTpButton.MouseButton1Click:Connect(function()
	autoTp = not autoTp

	autoTpButton.Text = "Auto Teleport: " .. (autoTp and "ON" or "OFF")

	if autoTp then
		task.spawn(function()
			while autoTp do
				teleportToPlayer()
				task.wait(5)
			end
		end)
	end
end)
